# Build output
dist/

# Dependencies
node_modules/

# Vercel CLI project linkage (created by `vercel link` / `vercel`)
.vercel

# Local env overrides (never commit; VITE_* values are set in Vercel)
.env*
*.local
